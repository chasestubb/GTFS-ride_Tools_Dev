# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
/frontend/node_modules
/backend/node_modules

# for our project, it is not a big deal to have the
#  package-lock in the gitignore. It sometimes gave
#  us nasty merge conflicts too.
/backend/package-lock.json
/frontend/package-lock.json

# testing
/coverage

# production
/build
/frontend/build
/backend/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ignore the feeds the user uploads (server storage)
/backend/uploads/*

# ignore the feeds created by feed creation
/backend/feed_creation/* 
